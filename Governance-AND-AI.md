# قوانین، هوش مرکزی و امنیت ثریا

## اصل حاکمیت
- مالک/سایت می‌تواند در هر زمان قوانین، نرخ‌ها، پاداش‌ها و سازوکار را تغییر دهد.
- تغییرات از مرکزیت اصلی و مراکز شهری ابلاغ می‌شوند.
- همهٔ ماژول‌ها (کیف، صرافی، بازی، پشتیبانی) موظف به دریافت و اعمال نسخهٔ قوانین هستند.

## هوش مرکزی (Suraya Central Intelligence)
نقش:
- مدیریت یکپارچهٔ سیاست‌ها و قوانین
- دریافت گزارش‌های عامل‌های محلی (با رضایت کاربر)
- یادگیری تدریجی از الگوهای استفاده (جمع‌آوری‌شده و ناشناس‌سازی‌شده در حد امکان)
- صدور دستورالعمل به اپ‌ها و صفحات مخاطب
- اولویت: امنیت، پایداری شبکه، تجربهٔ کاربری

همهٔ هوش‌های داخل برنامه‌ها به این مرکز متصل می‌شوند و وضعیت را همگام می‌کنند.

## عامل محلی روی دستگاه کاربر (Suraya Edge Agent)
- فقط با **اجازهٔ صریح کاربر** به امکانات دستگاه دسترسی دارد.
- هدف: کمک به استفادهٔ راحت‌تر، پیشنهاد بهتر، تشخیص مشکل فنی.
- می‌تواند از ابزارهای در دسترس همان دستگاه (در حد مجاز سیستم‌عامل) برای بهبود تجربه استفاده کند.
- دادهٔ لازم را به مرکز می‌فرستد؛ دستورات مجاز را از مرکز می‌گیرد و اجرا می‌کند.
- روی دستگاه‌ها و تکنولوژی‌های مختلف باید قابل تطبیق و یادگیری باشد (موبایل، وب، دسکتاپ).

## امنیت
- بدون رضایت کاربر: دسترسی به حسگرها، فایل‌ها، مخاطبین و ابزارهای حساس ممنوع است.
- کلیدهای خصوصی کیف فقط روی دستگاه کاربر یا به روش امن نگهداری می‌شود؛ مرکز کلید خصوصی را نگه نمی‌دارد.
- همگام‌سازی داده با رمزنگاری در انتقال انجام می‌شود.
- هدف «رتبهٔ نخست جهانی» با کیفیت، اعتماد و نوآوری دنبال می‌شود؛ نه با نقض حریم خصوصی.

## نسخهٔ قوانین
هر تغییر قانون یک شماره نسخه دارد. کلاینت‌ها قبل از عملیات حساس، نسخه را چک می‌کنند.
